<h1>Categories#index</h1>
<p>Find me in app/views/categories/index.html.erb</p>

<% @categories.each.with_index(1) do |obj, index| %>

    <tr>
    <td scope="row"><%= index %> - </td>
    <td><%= link_to "#{obj.name}", obj, class: "btn-sm btn-danger" %></td>
    </tr>

<% end %>